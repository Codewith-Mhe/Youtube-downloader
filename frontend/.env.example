# URL of the FastAPI backend.
# Local dev defaults to http://127.0.0.1:8000 if not set.
BACKEND_URL=http://127.0.0.1:8000

# Public site URL (used for SEO meta tags, sitemap, OpenGraph).
NEXT_PUBLIC_SITE_URL=http://localhost:3000
